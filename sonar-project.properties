# SonarCloud project configuration.
# organization/projectKey come from the SonarCloud "Import project" wizard
# (Sonar > + > Analyze new project > choose this GitHub repo) — replace the
# two values below with what SonarCloud generates if they differ.
sonar.organization=creditmutuelarkea
sonar.projectKey=CreditMutuelArkea_llm-inference

sonar.sources=llm_inference
sonar.tests=tests
sonar.python.version=3.10

sonar.python.coverage.reportPaths=coverage.xml
